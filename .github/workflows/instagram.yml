name: Instagram Monitor

on:
  schedule:
    - cron: "*/30 * * * *"   # 30分ごと
  workflow_dispatch:

jobs:
  instagram:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python & Instaloader
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install instaloader

      - name: Prepare session file
        run: |
          echo "${{ secrets.INSTALOADER_SESSION }}" | base64 --decode > session-atonofestival
          ls -l session-atonofestival

      - name: Download posts and stories
        run: |
          mkdir -p downloads
          python3 -m instaloader nakamu_sl \
            --stories \
            --sessionfile ./session-atonofestival \
            --latest-stamps stamps.txt \
            --dirname-pattern downloads/{target}

      - name: Detect updates
        id: changes
        run: |
          if [ "$(ls -A downloads 2>/dev/null)" ]; then
            echo "updated=true" >> $GITHUB_OUTPUT
          else
            echo "updated=false" >> $GITHUB_OUTPUT
          fi

      - name: Send Discord notification
        if: steps.changes.outputs.updated == 'true'
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content":"<@&1338644043243978792> 📸 nakamu_sl updated\nhttps://instagram.com/nakamu_sl"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
